'use strict';

angular.module('finalProjectApp')
  .controller('GamesRouteCtrl', function ($scope) {
    $scope.message = 'Hello';
    // $('body').append(events.event.away_team.full_name, events.event.home_team.full_name)
  });
